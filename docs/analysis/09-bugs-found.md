# באגים ובעיות שנמצאו — דורשים תיקון

**תאריך:** 7 באפריל 2026  
**מקור:** דיווח לקוחה + ניתוח קוד

---

## באג 1: תאריכי חגים שגויים

**קובץ:** `src/components/calendar/AdvanceWarningPanel.jsx` שורות 9-21  
**קובץ נוסף:** `src/pages/LifeSettings.jsx` (חגים hardcoded)

### מה שגוי:

| חג | תאריך במערכת | תאריך נכון | בעיה |
|----|-------------|------------|------|
| שביעי של פסח | 2026-04-09 | **2026-04-08** | יום אחד הפרש |
| יום העצמאות | 2026-04-15 | **2026-04-22** | שבוע שלם הפרש! |
| יום הזיכרון | לא קיים | **2026-04-21** | חסר לגמרי |

### תאריכי חגים נכונים 2026:
```
פורים:            2026-03-17 (י"ז באדר תשפ"ו)
פסח - ערב חג:     2026-04-01
פסח א':           2026-04-02
שביעי של פסח:     2026-04-08
יום הזיכרון:       2026-04-21 (חסר!)
יום העצמאות:       2026-04-22
שבועות:           2026-05-22
ראש השנה א':      2026-09-12
ראש השנה ב':      2026-09-13
יום כיפור:        2026-09-21
סוכות א':         2026-09-26
שמחת תורה:        2026-10-03
```

### הבעיה המבנית — 4 עותקים לא מסונכרנים:

| קובץ | מצב |
|-------|------|
| `AdvanceWarningPanel.jsx` שורה 9 | **שגוי** — שביעי פסח, יום העצמאות שגויים, חסר יום הזיכרון |
| `LifeSettings.jsx` שורה 25 | נכון אבל hardcoded ל-2026 |
| `TimeAwareness.jsx` שורה 22 | נכון אבל hardcoded ל-2026 |
| `HolidayAnalyzer.jsx` שורה 9 | **שגוי לחלוטין — עדיין חגי 2024!** |

### תיקון נדרש — פתרון מבני:
1. **ליצור קובץ מרכזי אחד:** `src/config/israeliHolidays.js` — כל 4 הקבצים מייבאים ממנו
2. **פתרון ארוך טווח — ייבוא אוטומטי:** להשתמש ב-Hebcal API שמחזיר חגים עבריים לפי שנה — כך גם 2027, 2028 וכל שנה מכוסים ללא עדכון ידני
3. **fallback:** cache מקומי של השנה האחרונה שנשלפה — למקרה שאין אינטרנט

---

## באג 2: מס"ב ספקים — דדליין שגוי

**קובץ:** `src/config/taxCalendar2026.js` שורה 77

**מצב נוכחי:** `masavSuppliersDay = 10` (10 לחודש)

**נכון:** מס"ב ספקים דדליין הוא **15 או 30 לחודש**, עם הכנה 3 ימי עבודה קודם.

### תיקון נדרש:
1. לשנות `masavSuppliersDay` מ-10 ל-**15** (או 30, תלוי בהגדרת הלקוח)
2. להוסיף אפשרות ל-2 מועדים (15 או 30)
3. להוסיף חישוב "3 ימי עבודה לפני" כאזהרה מוקדמת

---

## באג 3: המערכת מתעלמת מימי שישי-שבת

**קובץ:** `src/config/taxCalendar2026.js` שורות 26-31

**בעיה:** הפונקציה `adjustForRestDay` מזיזה רק אם הדדליין נופל על שישי/שבת/ראשון. אבל:
- **לא מזיזה כשנופל על חג** (למשל 15/04 שהמערכת חושבת שהוא יום העצמאות)
- **לא מביאה בחשבון שישי-שבת בחישוב "ימי עבודה"** — כשמחשבים 3 ימי הכנה לפני דדליין, צריך לדלג על שישי-שבת

### תיקון נדרש:
1. לתקן `adjustForRestDay` כך שידלג גם על חגים ישראליים
2. להוסיף פונקציה `getWorkDaysBefore(date, count)` שמחשבת ימי עבודה לפני תאריך (דילוג על שישי-שבת-חג)

---

## באג 4: פתקים דביקים לא מתעדכנים

**קובץ:** `src/pages/Home.jsx` — פונקציית `handleEditTask`

**בעיה:** כשמשימה מסומנת כהושלמה, הפתק הדביק נמחק מה-DB (דרך `syncNotesWithTaskStatus`) אבל ה-UI לא מתרענן כי `loadStickyNotes()` לא נקרא.

### תיקון נדרש:
להוסיף `loadStickyNotes()` אחרי `loadData()` בפונקציית `handleEditTask`.

---

## באג 5: משיכה כפולה של "משימות ממתינות לחומרים"

**מקור:** צילום מסך — "10 משימות ממתינות לחומרים" מופיע כתובנה מהמערכת

**בעיה:** ייתכן שה-Task Insights מציג סטטוס "ממתין לחומרים" פעמיים — פעם כתובנה ופעם כסיכום. לבדוק:
- `src/engines/taskCascadeEngine.js` — פונקציית `computeInsights`
- `src/hooks/useTaskCascade.js` — מה מוחזר ל-Home
- האם אותו נתון מוצג גם ב-Insights וגם ב-OverdueAlert

### תיקון נדרש:
לבדוק ולמנוע הצגה כפולה של אותו מידע.

---

## בעיה 6: כרטיסי סיכום ללא הפניות

**מקור:** לקוחה — "כרטיסים עם סיכומים ללא הפניות — תחושה של חרטה"

**בעיה:** כרטיסי Task Insights (למשל "3 תלושים קטנים - שעת כוח", "99/255 משימות הושלמו") מציגים מידע אבל **אין כפתור שמוביל לעמוד הרלוונטי**. המשתמשת רואה את הנתון אבל לא יכולה לעשות איתו כלום.

### תיקון נדרש:
- כל כרטיס Insight צריך **קישור/כפתור** — "הצג משימות" / "עבור לדשבורד שכר" / "פתח רשימת לקוחות"
- לחיצה מובילה לעמוד הרלוונטי עם סינון מתאים

---

## בעיה 7: חסר חיבור בין כרטיס לפעולה

**מקור:** לקוחה — "אין חיבור לשם מקום נוסף להכל"

**בעיה:** כרטיסי סיכום ותובנות הם "מידע מת" — אין דרך להגיע מהם לעמוד שבו אפשר לטפל בנושא.

**קשור לסעיפים:** שלב ו' (6.4 — דשבורד סטטוסים חי) בתוכנית המאסטר כבר מתייחס לזה. אבל צריך גם:
- **בדף הבית:** כל כרטיס insight = קישור ישיר
- **בדשבורדים:** כפתורי "צפה ברשימה" ליד כל סיכום

---

## סדר עדיפות לתיקונים

| עדיפות | באג | מורכבות | השפעה |
|---------|-----|---------|-------|
| 🔴 1 | חגים שגויים + יום הזיכרון | קלה | דדליינים שגויים — קריטי |
| 🔴 2 | מס"ב ספקים דדליין | קלה | דדליין שגוי — קריטי |
| 🔴 3 | התעלמות משישי-שבת-חג | בינונית | חישובי הכנה שגויים |
| 🟡 4 | פתקים דביקים | קלה (שורה אחת) | UX |
| 🟡 5 | כרטיסים ללא הפניות | בינונית | UX — "חרטה" |
| 🟡 6 | משיכה כפולה | צריך בדיקה | UX |
| 🟢 7 | חיבור כרטיס→פעולה | בינונית | חלק משלב ו' |

---

## באגים שתוקנו בסשן 12/04/2026 — branch `claude/add-injection-buttons-filtering-9InNw`

### באג 8: ✅ ClientWorkbook קורס בלשונית פירוט משימות
**קומיט:** `af18548` · **קובץ:** `src/pages/ClientWorkbook.jsx`
- Radix `<SelectItem value="">` זרק חריגה. הוחלף ב-`__empty__` sentinel ב-`COMPLEXITY_OPTIONS`.

### באג 9: ✅ לשונית "חשבונות בנק" בכרטיס לקוח הציגה שדות שגויים
**קומיט:** `af18548` · **קובץ:** `src/pages/ClientWorkbook.jsx`
- היה מציג שדות מטא של לקוח. עובר עכשיו לטעון `ClientAccount` (account_type='bank') עם השדות הנכונים: account_name, bank_name, account_number, bookkeeping_card_number, loading_system, reconciliation_frequency.

### באג 10: ✅ לשוניות בעברית נטענו LTR במקום RTL
**קומיט:** `af18548` · **קובץ:** `src/pages/ClientWorkbook.jsx`
- נוסף `dir="rtl"` ל-Tabs + צבע ייחודי לכל לשונית (emerald/blue/purple/amber/cyan).

### באג 11: ✅ ClientCard — טקסט לבן בלתי קריא בכותרות שירות
**קומיט:** `af18548` · **קובץ:** `src/components/clients/ClientCard.jsx`
- חולשת ניגודיות. חוזק ל-`bg-sky-700` / `bg-indigo-700` / `bg-amber-700` / `bg-emerald-700`.

### באג 12: ✅ ClientManagement — רשימת לקוחות אינסופית (mageelat Esther)
**קומיט:** `af18548` · **קובץ:** `src/pages/ClientManagement.jsx`
- הוסף עימוד חכם פר-status: 15 גלויים + כפתור "הצג עוד" + Badge מונה. מאופס בכל שינוי חיפוש/סינון.

### באג 13: ✅ Tasks (כל המשימות) — סדר תצוגות לא הגיוני + חסר סינון domain
**קומיט:** `af18548` · **קובץ:** `src/pages/Tasks.jsx`
- סדר חדש: טבלה → גאנט → מיינדמפ → זרימה → רשימה → קנבן → סדנה → מיקוד → מירו.
- ברירת מחדל שונתה ל-"table".
- הוספת רצועת לשוניות domain (שכר / הנה"ח ומיסים / מאזנים / ניהול) מעל הסינונים, עם מונה קטגוריות, שמצמצמת את התפריט לפי הבחירה.

### באג 14: ✅ הזרקת משימות דו-חודשיות גם כשאין דיווח דורש
**קומיט:** `af18548` · **קבצים:** `src/engines/recurringTaskEngine.js`, `src/config/companyProcessTree.js`
- **תופעה:** "קליטת הכנסות/הוצאות/התאמות" יוצרו אוטומטית גם בחודשים שאין שום דיווח (מע"מ/מקדמות/רו"ה) שמושך מהן.
- **תיקון:** סומנו `is_data_collection: true` על P2_income/P2_expenses/P2_reconciliation. הוסף guard `hasActiveDependentThisMonth()` שבודק אם יש לפחות צרכן downstream פעיל ולא off-month — אחרת מדלג על ההזרקה.

### באג 15: ✅ Tasks.jsx merge conflict עם main (תצוגת table חדשה)
**קומיט:** `a935f21` · **קובץ:** `src/pages/Tasks.jsx`
- main הוסיף תצוגת `table` חדשה (GroupedServiceTable) שאינה זהה ל-`list`. הסיכסוך נפתר על ידי שמירת שתי התצוגות והחלת הסדר החדש.

### באג 16: ✅ ProcessTreeManager override מתעלם בהזרקה (קריטי)
**קומיט:** `d8fe209` · **קובץ:** `src/components/clients/ClientRecurringTasks.jsx`
- **תופעה דווחה:** לקוח שעודכן ל-monthly ביטוח לאומי דרך ProcessTreeManager לא הופיע בהזרקה לחודש בודד; לקוחות bimonthly המשיכו להופיע.
- **שורש הבעיה:** `getClientFrequency()` קרא רק `client.reporting_info[frequencyField]`, לא את ה-override החדש שנשמר ב-`client.process_tree[treeNodeId].frequency` על ידי ProcessTreeManager.
- **תיקון:** הוסף שלב 1 ב-resolution chain — קודם קוראים `client.process_tree[cat.treeNodeId].frequency` (אם קיים ושונה מ-'inherit'/'not_applicable'), ורק אז fallback ל-reporting_info הישן. תואם ל-`resolveFrequency()` ב-`processTreeService.js:1213`.
